# 11.24将Snail编译类型改为静态库SLL

主要修改：premake.lua & Core.h

premake.lua 

>kind "StaticLib"
>-- kind "SharedLib"

>"SNL_BUILD_SLL"
>-- "SNL_BUILD_DLL"

> -- ("{COPYFILE} ../bin/" .. outputdir .. "/Snail/Snail.dll %{cfg.targetdir}")

Core.h

>#ifdef SNL_PLATFORM_WINDOWS
>#ifdef SNL_BUILD_DLL
>	#define SNAIL_API \_\_declspec(dllexport)
>#else
>	#define SNAIL_API \_\_declspec(dllimport)
>#endif#endif

>#ifdef SNL_PLATFORM_WINDOWS
>#ifdef SNL_BUILD_DLL
>	#define SNAIL_API \_\_declspec(dllexport)
>#elif SNL_BUILD_SLL
>	#define SNAIL_API
>#else
>	#define SNAIL_API
>#endif#endif